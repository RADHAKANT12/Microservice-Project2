server:
  port: 8888
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RADHAKANT12/Micro2-Config-Server
          clone-on-start: true
          default-label: main





eureka:
  instance:
    client:
      serverUrl:
        defaultZone:  http://localhost:8761/eureka/